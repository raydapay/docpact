# TY002 — Returns section is 'None.' but return annotation is non-None

**Namespace:** `TY`  
**Severity:** 🟡 warning  
**Fix:** No automatic fix

---

## Description

<!-- TODO: expand the description for TY002 -->

## Examples

### Triggering

```python
# TY002 fires here — add a concrete example
```

### Passing

```python
# TY002 does not fire here — add a concrete example
```

## Configuration

This rule can be disabled with:

```toml
[tool.docpact]
ignore = ["TY002"]
```

Or suppressed inline:

```python
def my_function(  # nodo: TY002 -- reason
    ...
```
