# SEM002 — Module docstring is semantically weak (advisory; module scan)

**Namespace:** `SEM`  
**Severity:** 🟡 warning  
**Fix:** No automatic fix

---

## Description

<!-- TODO: expand the description for SEM002 -->

## Examples

### Triggering

```python
# SEM002 fires here — add a concrete example
```

### Passing

```python
# SEM002 does not fire here — add a concrete example
```

## Configuration

This rule can be disabled with:

```toml
[tool.docpact]
ignore = ["SEM002"]
```

Or suppressed inline:

```python
def my_function(  # nodo: SEM002 -- reason
    ...
```
