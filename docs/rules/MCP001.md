# MCP001 — Both decorator description= and docstring MCP: section present

**Namespace:** `MCP`  
**Severity:** 🔴 error  
**Fix:** Safe fix (`--fix`) and unsafe fix (`--unsafe-fixes`) available

---

## Description

<!-- TODO: expand the description for MCP001 -->

## Examples

### Triggering

```python
# MCP001 fires here — add a concrete example
```

### Passing

```python
# MCP001 does not fire here — add a concrete example
```

## Configuration

This rule can be disabled with:

```toml
[tool.docpact]
ignore = ["MCP001"]
```

Or suppressed inline:

```python
def my_function(  # nodo: MCP001 -- reason
    ...
```
