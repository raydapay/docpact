# DOC021 — "Defaults to X" in Args prose does not match the signature default

**Namespace:** `DOC`  
**Severity:** 🟡 warning  
**Fix:** No automatic fix

---

## Description

<!-- TODO: expand the description for DOC021 -->

## Examples

### Triggering

```python
# DOC021 fires here — add a concrete example
```

### Passing

```python
# DOC021 does not fire here — add a concrete example
```

## Configuration

This rule can be disabled with:

```toml
[tool.docpact]
ignore = ["DOC021"]
```

Or suppressed inline:

```python
def my_function(  # nodo: DOC021 -- reason
    ...
```
